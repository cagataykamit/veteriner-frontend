/** Klinik oluşturma / güncelleme — form `getRawValue()` çıktısı. */
export interface ClinicUpsertFormValue {
    name: string;
    city: string;
    phone: string;
    email: string;
    address: string;
    description: string;
}
