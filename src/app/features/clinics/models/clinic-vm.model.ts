export interface ClinicListItemVm {
    id: string;
    name: string;
    city: string;
    isActive: boolean | null;
}

export interface ClinicDetailVm {
    id: string;
    name: string;
    city: string;
    isActive: boolean | null;
}
